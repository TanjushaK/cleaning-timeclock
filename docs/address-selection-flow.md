# Site address selection

For new and edited sites, the administrator enters a street and house number and selects one exact Dutch address from the suggestion list.

The selected formatted address, latitude and longitude are saved together. Editing the address invalidates the previous selection and clears stale coordinates until another suggestion is selected.

The server must not silently replace a confirmed selection with the first geocoding result.
