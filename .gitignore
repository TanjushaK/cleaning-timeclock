# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.zip

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
.env*.local
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# локальный PostgreSQL пользовательского кластера (initdb/pg_ctl), большие binary файлы данных
/var/postgres-bundle/data/

# Локальный рантайм self-host (логи, загрузки — не коммитить)
/var/logs/
/var/uploads/
# Повторяем без ведущего слеша (некоторые клиенты Git на Windows)
var/logs/
var/uploads/

# Временные логи smoke / локального `npm run start` (при появлении)
var/logs/local-start*.log
var/logs/curl-*.txt

# Одноразовые снимки аудита / заметки IDE (при необходимости восстановите вручную)
/audit-reports/
/release-audit/
CURSOR_TASK.md

# Локальные бинарные артефакты деплоя/бэкапа
*.dump
*.tgz
var/imports/*.dump
