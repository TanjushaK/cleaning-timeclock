# BASELINE 2026-04-23

- date: 2026-04-23
- branch: `selfhost-audit-green`
- commit_sha: `4b32611ce4f6e1e6e70b61f1310117e8585a4a40`
- tag: `baseline-2026-04-23`
- production_url: `https://timeclock.tanjusha.nl`
- server_path: `/opt/timeclock`
- service: `timeclock.service`

## Local green checks (this baseline cycle)

- `npm install` — OK (up to date)
- `npm run build` — OK (Next.js 16.2.3, TypeScript OK)
- `npm run lint` — OK
- `local-smoke-auth-api.ps1` — OK (GET pages 200; login wrong password 401; refresh 400 without valid token; geocode 200)

## Production green checks (preflight)

- `timeclock.service` active
- PostgreSQL active (`postgresql`, `postgresql@16-main`)
- `127.0.0.1:5432` listening
- `GET /` = 200
- `GET /forgot-password` = 200
- `GET /reset-password` = 200
- `GET /api/auth/sms-capabilities` = 200
- `POST /api/auth/login` wrong password = 401 (`AUTH_INVALID_CREDENTIALS`)
- `POST /api/auth/forgot-password` = 200 (`{"ok":true,"delivery":"none"}`)

## Known warnings

- Device verification required for Face ID / Touch ID / fingerprint on physical devices.
- App Store metadata must avoid overclaiming biometric-only login.

## Baseline scope

This baseline corresponds to self-host runtime + auth/recovery + biometric integration + cleanup/audit docs, with safe deploy constraints to protect environment and runtime data paths.

## GitHub state

- PR to `main`: `https://github.com/TanjushaK/cleaning-timeclock/pull/38` (open, blocked by required checks/rules)
- `main` merged baseline: no
